{
  "name": "metalsmith-pathnames",
  "version": "1.0.0",
  "description": "Adds path and filename to metadata",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andy Burke (andy.burke@mailbox.earth)",
  "license": "MIT",
  "dependencies": {
    "extend": "^3.0.1",
    "minimatch": "^3.0.4"
  }
}
